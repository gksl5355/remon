# ============================================================
# REMON 환경 설정 예제 파일
# ============================================================
# 본 파일을 복사하여 .env 파일을 생성하세요:
#   cp .env.example .env
# 그 후 각 값을 프로젝트에 맞게 수정하세요.
#
# 주의: 모든 상대경로는 프로젝트 루트(/home/minje/remon)를 기준으로 합니다.
#      git clone 후 다른 경로에서 실행해도 자동으로 상대경로로 동작합니다.
# ============================================================

# ==================== 데이터베이스 ====================
# PostgreSQL 데이터베이스 연결 URL
DATABASE_URL=sqlite:///./data/remon.db
# 또는 PostgreSQL: postgresql+asyncpg://user:password@localhost:5432/remon

# ==================== Redis (선택사항) ====================
# Redis 연결 URL (비동기 처리에 사용, 없으면 순차 처리)
# REDIS_URL=redis://localhost:6379
ENABLE_REDIS_ASYNC=false

# ==================== 보안 ====================
# JWT 서명용 시크릿 키 (프로덕션에서는 강력한 랜덤 문자열 사용)
SECRET_KEY=your-secret-key-here-change-in-production

# ==================== VectorDB (Chroma) ====================
# Chroma 저장소 경로 (상대경로, 프로젝트 루트 기준)
CHROMA_DB_PATH=./data/embeddings
# Chroma 컬렉션 이름
CHROMA_COLLECTION=remon_regulations

# ==================== OpenAI ====================
# OpenAI API 키 (필수)
OPENAI_API_KEY=sk-your-openai-api-key-here
# OpenAI 모델 지정 (기본값: gpt-4o-mini)
OPENAI_MODEL_PROPOSITION=gpt-4o-mini
OPENAI_MODEL_ANSWER=gpt-4o-mini
# 타임아웃 (초)
OPENAI_TIMEOUT=30

# ==================== LangSmith (추적/디버깅, 선택사항) ====================
# LangSmith 추적 활성화 (true/false)
LANGCHAIN_TRACING_V2=false
# LangSmith API 키 (LANGCHAIN_TRACING_V2=true일 때 필수)
# LANGCHAIN_API_KEY=your-langsmith-api-key
# LangSmith 프로젝트명
LANGCHAIN_PROJECT=remon-advanced-rag

# ==================== 로깅 ====================
# 로그 레벨: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
# 디버그 모드 (verbose 출력, true/false)
DEBUG_MODE=false

# ==================== 개발 환경 ====================
# FastAPI 개발 서버 재로드 (uvicorn --reload)
RELOAD=true
# FastAPI 서버 호스트 & 포트
HOST=127.0.0.1
PORT=8000

# ==================== Preprocess 모듈 (선택사항, config.py에서 오버라이드 가능) ====================
# 하이브리드 검색 가중치 (0-1, 0.5 = 50% 의미+50% BM25)
# HYBRID_ALPHA=0.5
# 테이블 포함 청크 점수 부스트
# TABLE_BOOST=1.3
# 카테고리 매칭 점수 부스트
# CATEGORY_BOOST=1.3
