<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <style>
    :root {
      --main: #1f3a5f;
      --accent: #c7d2e3;
      --text: #1a1a1a;
      --muted: #6b7280;
      --bg-soft: #f4f6fa;
    }

    /* ===============================
       PAGE (PRINT)
    =============================== */
    @page {
      margin: 18mm 18mm 16mm 18mm; /* ⬅ 상단/하단 축소 */
    }

    /* ===============================
       BASE
    =============================== */
    body {
      font-family: "Noto Sans CJK KR", "Noto Sans KR", "Malgun Gothic",
                   "Apple SD Gothic Neo", Arial, sans-serif;
      font-size: 11.5px;
      line-height: 1.7;
      color: var(--text);
      margin: 0;
      padding: 0;

      word-break: keep-all;
      overflow-wrap: break-word;
    }

    .container {
      max-width: 900px;
      margin: 0 auto;
      padding: 6mm 6mm 4mm 6mm; /* ⬅ 핵심 */
    }

    /* ===============================
       HEADER
    =============================== */
    header {
      border-bottom: 1.5px solid var(--accent);
      padding-bottom: 10px;
      margin-bottom: 16px;
    }

    h1 {
      font-size: 21px;          /* ⬅ 축소 */
      color: var(--main);
      margin: 0 0 4px 0;
      letter-spacing: 0.2px;
      font-weight: 700;
    }

    .meta {
      font-size: 11px;
      color: var(--muted);
    }

    /* ===============================
       LAW STRUCTURE
    =============================== */

    /* PART */
    h2 {
      font-size: 16px;
      margin: 22px 0 12px 0;
      padding: 8px 12px;
      background: var(--bg-soft);
      border-left: 5px solid var(--main);
      font-weight: 700;
    }

    /* Subpart */
    h3 {
      font-size: 14px;
      margin: 18px 0 8px 0;
      padding-bottom: 5px;
      border-bottom: 1px solid var(--accent);
      font-weight: 600;
    }

    /* Section */
    h4 {
      font-size: 13px;
      margin: 14px 0 6px 0;
      font-weight: 700;
      color: var(--main);
    }

    /* ===============================
       PARAGRAPH
    =============================== */
    p {
      margin: 5px 0;
      white-space: normal;
    }

    /* (a)(b)(c) */
    p strong {
      font-weight: 600;
    }

    p:has(> strong:first-child) {
      padding-left: 14px;
      text-indent: -14px;
    }

    /* ===============================
       TABLE / CODE
    =============================== */
    table {
      width: 100%;
      border-collapse: collapse;
      margin: 12px 0 16px 0;
      page-break-inside: avoid;
      font-size: 12px;
    }

    th, td {
      border: 1px solid var(--accent);
      padding: 7px 9px;
      text-align: left;
      vertical-align: top;
    }

    th {
      background: #eef1f7;
      font-weight: 600;
    }

    /* ===============================
       FOOTER
    =============================== */
    .divider {
      margin: 18px 0 10px 0; /* ⬅ 축소 */
      border: 0;
      border-top: 1px solid var(--accent);
    }

    .footer {
      text-align: center;
      font-size: 10px;
      color: var(--muted);
      margin-top: 4px;
    }
  </style>
</head>

<body>
  <div class="container">
    <header>
      <h1>{{ title | default("번역본") }}</h1>
      <div class="meta">
        {% set current_year = datetime.now().year %}
        번역본 (KO) · {{ current_year }}
      </div>
    </header>

    <div>
      {{ body | safe }}
    </div>

    <hr class="divider">

    <p class="footer">
      © {{ current_year }} Regulatory Translation Document
    </p>
  </div>
</body>
</html>
